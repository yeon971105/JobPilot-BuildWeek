# Validated combined output

`npm run study:combine -- build-week/study/inbox` writes `combined.csv` and its combination manifest here only after every inbox record passes validation.

Both generated files are intentionally ignored by Git because a future authentic run contains anonymous participant-level records. Only aggregate public-safe summaries belong in the public repository.
