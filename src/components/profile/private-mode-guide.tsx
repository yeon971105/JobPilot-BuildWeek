import Link from "next/link";
import { LockKeyhole, Terminal } from "lucide-react";
import { DemoHeader } from "@/components/demo/demo-header";

export function PrivateModeGuide({ enabled }: { enabled: boolean }) {
  return <div className="min-h-screen bg-[#fbf7ed] text-[#173d2d]"><DemoHeader /><main className="mx-auto max-w-4xl px-5 py-10"><p className="eyebrow"><LockKeyhole className="size-4" /> Local edition</p><h1 className="mt-3 font-serif text-5xl">Private Resume Mode</h1><p className="mt-4 max-w-3xl text-lg leading-8 text-[#587064]">PDF, DOCX, and TXT files are parsed in memory by the local JobPilot server. Confirmed structured evidence is matched by Gemma 4 12B on loopback Ollama; deterministic code calculates the score.</p><div className="paper-card mt-8"><div className="flex items-center gap-3"><Terminal className="size-5 text-[#a1742d]" /><h2 className="font-serif text-3xl">Local setup</h2></div><ol className="mt-5 space-y-4 text-sm leading-7 text-[#587064]"><li>1. Install dependencies with <code>npm install</code>.</li><li>2. Install Ollama and make sure <code>gemma4:12b</code> is available locally.</li><li>3. Set <code>LOCAL_PRIVATE_MODE=true</code> and <code>OLLAMA_ENABLED=true</code>.</li><li>4. Start JobPilot with <code>npm run dev</code> or a production build.</li></ol><p className="mt-5 rounded-xl bg-[#edf4ea] p-4 text-sm font-bold">Current runtime: {enabled ? "Local Private Mode enabled" : "Public Judge Mode — uploads disabled"}</p><Link href="/profile/resume" className="button-primary mt-6">Open Resume Onboarding</Link></div><section className="paper-card mt-5"><h2 className="font-serif text-2xl">Privacy guarantees</h2><ul className="mt-4 space-y-2 text-sm leading-6"><li>• No resume transmission to OpenAI or another cloud model.</li><li>• No raw resume bytes or text persisted by default.</li><li>• No raw resume content in logs, reports, or Score Receipts.</li><li>• No protected-attribute inference or scoring.</li><li>• No application submission.</li></ul></section></main></div>;
}
